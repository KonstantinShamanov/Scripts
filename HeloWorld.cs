using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HeloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //команда напечатает привет мир.
        print ("Hello, World") ;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
